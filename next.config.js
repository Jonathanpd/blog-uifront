/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'apiuifront.local',
      'api.uifront.com',
      'tailwindui.com',
      'flowbite.com'
    ]
  }
}

module.exports = nextConfig
